const merge = require('webpack-merge');
const common = require('./common');
const path = require('path');

module.exports = merge(common, {
  mode: 'production',
  target: 'es5',
  output: {
    path: path.resolve(__dirname, '../dist'),
    filename: 'dependentree.es5.min.js',
  },
});
